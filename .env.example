# SoloCloud 环境配置文件
# 复制此文件为 .env 并根据你的环境修改配置

# 生成随机的SECRET_KEY: python3 -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# Flask环境 (development/production)
FLASK_ENV=development

# 日志级别
LOG_LEVEL=DEBUG

# ===========================================
# 可选配置 (如需覆盖默认值)
# ===========================================
# 系统会自动检测是否在Docker中运行，并使用相应的默认路径
# 如果需要自定义路径，可以取消注释下面的配置

# 数据库文件路径 (默认: 本地=data/SoloCloud.db, Docker=/app/data/SoloCloud.db)
# DATABASE_URL=sqlite:///your/custom/path/SoloCloud.db

# 上传文件目录 (默认: 本地=uploads, Docker=/app/uploads)
# UPLOAD_FOLDER=your/custom/uploads

# 日志文件路径 (默认: 本地=logs/SoloCloud.log, Docker=/app/logs/SoloCloud.log)
# LOG_FILE=your/custom/logs/app.log

# 存储配置
# 可选值: local, aliyun_oss, tencent_cos, qiniu, jianguoyun
STORAGE_PROVIDER=local

# 阿里云OSS配置
ALIYUN_OSS_ACCESS_KEY_ID=your-access-key-id
ALIYUN_OSS_ACCESS_KEY_SECRET=your-access-key-secret
ALIYUN_OSS_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com
ALIYUN_OSS_BUCKET_NAME=your-bucket-name

# 腾讯云COS配置
TENCENT_COS_SECRET_ID=your-secret-id
TENCENT_COS_SECRET_KEY=your-secret-key
TENCENT_COS_REGION=ap-beijing
TENCENT_COS_BUCKET_NAME=your-bucket-name

# 七牛云配置
QINIU_ACCESS_KEY=your-access-key
QINIU_SECRET_KEY=your-secret-key
QINIU_BUCKET_NAME=your-bucket-name
QINIU_DOMAIN=your-domain.com

# 坚果云配置（WebDAV）
JIANGUOYUN_WEBDAV_URL=https://dav.jianguoyun.com/dav
JIANGUOYUN_USERNAME=your-username
JIANGUOYUN_PASSWORD=your-app-password
